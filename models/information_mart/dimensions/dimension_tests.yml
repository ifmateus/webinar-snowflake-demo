models:
  - name: dim_client #model name
    columns:
      - name: 'client_id' #column to test
        tests:
          - unique
          - not_null
      - name: 'client_type'
        tests:
        - accepted_values:
            values: [individual,company]
            quote: true